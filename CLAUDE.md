# 이 저장소에서 작업할 때의 규칙

Data Center Deep Dive — CS·데이터센터 인프라 기술 블로그.
Jekyll 로 만들고 GitHub Pages 가 `main` 브랜치를 그대로 빌드해 배포합니다.

---

## 다이어그램 (중요)

**본문의 주요 다이어그램은 손그림(hand-drawn) 스타일로 그립니다.** 이것이 이 블로그의 기본값입니다.
따로 요청이 없으면 항상 이 형식으로 만드세요.

### Mermaid 로 그리지 않습니다

시나리오 다이어그램(트래픽이 흐르는 그림)은 `tools/diagrams/` 의 **생성기로 SVG 를 만들어**
`_includes/diagrams/` 에 넣고, 본문에서 이렇게 불러옵니다.

```liquid
<div class="diagram">{% include diagrams/이름.svg %}</div>
```

Mermaid 로 되돌리지 마세요. 세 번 시도했고 세 번 다 글쓴이가 반려했습니다. 이유는 분명합니다.

| 요구 | Mermaid | 생성기 |
| --- | --- | --- |
| 주석을 상자 **바로 아래**에 붙이기 | dagre 가 단(rank) 단위로만 놓아 한참 아래로 떨어집니다 | 상자 밑 6px 에 고정 |
| 존을 건너는 화살표를 **직선**으로 | 곡선(basis)이고, 양끝 높이가 다르면 비스듬해집니다 | 모든 열을 한 중심선에 맞춰 언제나 수평 |
| 존 크기를 **내용에 맞추기** | 옆 존이 크면 따라 커집니다 | 존마다 자기 내용만큼 |
| 글자가 상자를 넘지 않기 | 임시 요소에서 폭을 재서 어긋납니다 | 실제 Gaegu 를 띄워 재고 그 폭으로 상자를 만듭니다 |

계층·분류처럼 흐름이 아닌 단순한 그림은 Mermaid(`mermaid: true`)를 그대로 써도 됩니다.
설정은 `_includes/mermaid.html` 에 있습니다.

### 그림 하나 만드는 법

1. `tools/diagrams/spec.js` 에 존과 상자를 적습니다. **좌표는 적지 않습니다.**
2. `cd tools/diagrams && npm install && npm run build`
3. 본문에서 `{% include %}` 로 불러옵니다.

```js
{
  id: 'nat-port-forwarding',
  layout: 'cols',                  // 존을 한 줄로. 여러 시나리오를 비교하려면 'compare' + `groups`
  alt: 'the client dials a public address and the firewall rewrites it',
  zones: [
    { title: 'your machine',
      cols: [[{ kind:'client', title:'client', sub:'(ex) 203.0.113.10:8000' }]] },
    { title: 'server side',
      cols: [
        [{ kind:'kernel', title:'firewall', sub:'(ex) :8000 \u2192 192.168.0.10:8100',
           note:'* set by the network admin' }],
        [{ kind:'server', title:'server', sub:'(ex) 192.168.0.10:8100' }],
      ] },
  ],
}
```

자세한 것은 [tools/diagrams/README.md](tools/diagrams/README.md).

### 역할별 색 규칙

참고한 원본 그림과 같은 색 약속을 씁니다. `spec.js` 의 `kind` 로 지정합니다.

| `kind` | 색 | 뜻 |
| --- | --- | --- |
| `client` | 초록 | 요청을 보내는 쪽 |
| `proxy` | 빨강 | 사용자 공간에서 연결을 대신 맺어주는 중개자 |
| `server` | 파랑 | 최종 목적지 |
| `kernel` | 빨간 점선 | 커널이 패킷을 고쳐 보내는 구간 (프로세스가 아님) |
| 존 | 회색 점선 | 서로 다른 망 (`your machine`, `server side`) |
| 주석 | 테두리 없는 회색 글씨 | 상자 밖 바로 아래 `* 누가 설정하는가` 한 줄 |

### 주석(`note`) — 상자 **밖** 바로 아래에

설정 주체는 **상자 안이 아니라 상자 밖 바로 아래**에 붙입니다. 이것이 그림의 핵심입니다.
`note` 를 적으면 그 상자 밑에 자동으로 붙습니다. 절대 상자 안(`sub`)으로 옮기지 마세요.

- 한 줄로. `* set by the network admin`, `* set by you — ssh -D 9999`, `* set by the server operator`
- 그 상자를 **누가 설정하는가**만 적습니다. `you point the app here` 처럼 동작 설명을 적지 않습니다.

### 상자 안(`sub`) — 실제 접속 주소 한 줄

상자에는 **이름 한 줄 + `(ex)` 한 줄**만 넣습니다. 그 상자가 실제로 어느 주소로 접속하는지,
또는 무엇을 어디로 넘기는지를 한 줄로 적습니다. 그 외의 설명은 넣지 않습니다.

- IP·포트는 문서용·사설 대역만 씁니다 — 공인 `203.0.113.x`, 사설 `192.168.0.x` / `10.0.0.x`
- URL 은 `http://my-url`
- 규칙이면 화살표로 — `(ex) :8000 → 192.168.0.10:8100`
- 같은 글 안의 여러 그림과 본문 코드(`proxy_pass` 등)에서 **주소를 똑같이** 씁니다.

### 다이어그램 안의 글자는 영어로

**박스 이름, 설명, 주석 — 다이어그램에 들어가는 글자는 전부 영어로 씁니다.**
본문이 한국어여도 그림만은 영어입니다.

- 한국어를 그대로 옮긴 어색한 영어가 되지 않게, **영어로 읽어서 자연스러운 표현**을 씁니다.
- 박스 이름은 **소문자 한두 단어**가 기본 (`client`, `proxy`, `server`, `nginx`, `firewall`).
  고유명사와 약어는 그대로 (`SOCKS proxy`, `SSH server`, `ToR switch`, `NIC`).
- 그림에 긴 설명을 욱여넣지 마세요. 그림은 뼈대만 보여주고, 설명은 본문(한국어)에서 풉니다.

나쁜 예: `["방화벽 · 공유기가 목적지 주소를 바꿈"]`
좋은 예: `{ title: 'firewall', sub: '(ex) :8000 → 192.168.0.10:8100' }`

### 용어는 그림끼리 일치시킵니다

한 글 안의 그림들은 **같은 말을 같은 이름으로** 씁니다. `your machine` / `server side` 처럼
존 이름을 통일하고, 어느 그림에서만 `remote server` 라고 부르는 식으로 흔들리지 않게 합니다.

### 회색 점선 존은 언제나 망(location) 입니다

**존은 `your machine` / `server side` 처럼 어느 망에 있는지만 뜻합니다.** 두 방식을
비교하는 그림에서 "port forwarding" / "proxy" 같은 **시나리오 이름을 존 제목에 넣지 마세요.**
존과 캡션은 다른 것입니다 — `layout: 'compare'` 의 `groups[].caption` 이 시나리오 이름이고,
그 안의 `zones` 는 여전히 `your machine` / `server side` 여야 합니다.

### legend 는 쓰지 않습니다

색과 점선의 뜻을 따로 설명하는 legend 다이어그램은 넣지 마세요.
망 이름과 `*` 한 줄이면 그림만 보고도 읽힙니다.

### 폭 — 가로 스크롤은 금지

**어떤 화면 폭에서도 가로로 스크롤하지 않고 한눈에 보여야 합니다.**
생성기가 `viewBox` 로 그리고 `width:100%` 로 줄이므로 구조상 넘칠 수 없습니다.
다만 상자가 많아지면 휴대폰에서 글씨가 작아지니, **한 열에 상자 3개, 한 그림에 존 2개**까지로 봅니다.

판은 본문(720px)보다 좌우로 넓게 잡되, 화면이 좁아지면 넓히는 양이 0 까지 자동으로
줄어듭니다(`--bleed: clamp(...)`). 이 값을 고정값으로 되돌리지 마세요. 중간 폭에서 넘칩니다.

### 알려진 함정

- 글자 폭은 **실제 Gaegu 를 브라우저에 띄워** 재야 합니다. 어림값으로 상자를 만들면 글씨가 넘칩니다.
- `document.fonts.ready` 만으로는 부족합니다. 아직 그 글꼴을 쓰는 요소가 없으면 받을 것이
  없다고 보고 즉시 끝나버립니다. `document.fonts.load('400 16px Gaegu')` 로 명시적으로 부릅니다.
- roughjs 의 `toPaths()` 는 점선(`strokeLineDash`) 정보를 넘겨주지 않습니다.
  `stroke-dasharray` 는 생성기에서 직접 붙입니다.
- 다이어그램은 흰 판 위에 올라가므로(`.diagram` 스타일) 라이트·다크 모두 같은 색으로 보입니다.
  색을 정할 때 다크 모드를 따로 고려하지 않아도 됩니다.
- **다이어그램 안의 글씨는 예외 없이 손글씨(Gaegu)** 입니다. 일부만 고정폭으로 바꾸지 마세요.

---

## 글 구조 (중요)

**[프록시와 포트 포워딩](_posts/ko/2026-09-16-proxy-and-port-forwarding.md) 글을 표준으로 삼습니다.**
새 글을 쓸 때 아래 틀을 그대로 따르세요. 이 절은 시행착오를 거쳐 나온 규칙입니다 —
비교표를 성급하게 앞에 뒀다가 뒤로 옮긴 일, note 가 너무 많아져 정리한 일이 실제로 있었습니다.

### 전체 뼈대

1. **여는 문단** — 질문 두세 개로 문제를 제기합니다. "정리해보니 질문 두 개로 갈리더군요"
   처럼 무엇을 기준으로 나뉘는지 먼저 던지고, 마지막 문장은 "하나씩 풀어보겠습니다" 같은
   전환으로 닫습니다. 2~4 문장. 결론이나 정의를 여기서 먼저 말하지 않습니다 — 뒤에서 풀어갈
   호기심을 남겨둡니다.
2. `<!--more-->` — 여는 문단 바로 뒤에 한 번. 이 앞부분이 목록·RSS 요약이 됩니다.
3. **번호 매긴 절** (`## 1.`, `## 2.` ...) — 큰 흐름 순서대로 놓습니다. 프록시 글은 이렇게 짰습니다.
   1. 두 핵심 개념의 차이 (정의 + 비교 다이어그램 하나)
   2. 각 방식을 하나씩 (`###` 소제목으로 쪼갬 — 방식이 여럿이면 절 하나에 다 몰아넣지 않습니다)
   3. 심화 — 헷갈리는 지점, 다른 개념과의 관계 (2번에서 다 다룬 뒤에)
   4. 정리 (비교표 + 한 줄 요약) — **항상 마지막 절**
4. 절 번호와 소제목만 훑어도 글의 논리가 보여야 합니다. 목차를 따로 만들지 않아도
   제목만으로 "차이 → 각론 → 심화 → 정리" 흐름이 읽혀야 합니다.

### 개념 하나를 설명하는 순서

`###` 소제목 하나가 다루는 범위는 이 순서를 따릅니다.

1. **정의 한두 문장** — 핵심 동작 원리를 먼저 말로 정리합니다.
2. **다이어그램** (있다면) — 말로 정리한 것을 그림으로 보여줍니다.
3. **그림 해설 문단** — 다이어그램에서 무엇을 봐야 하는지 짚어줍니다. 그림을 그대로
   말로 옮기지 말고 "여기서 짚을 점이 세 개 있습니다" 처럼 독자가 봐야 할 포인트를 추립니다.
4. **(필요하면) 설정 방법** — 실제로 어떻게 하는지. 서로 다른 두 종류의 설정(예: 터널을
   만드는 것과 앱이 그 터널을 쓰게 만드는 것)이 섞여 있으면 **1단계/2단계**로 쪼개 구분합니다.
   (Dynamic SSH 절의 "1단계 — 터널 만들기" / "2단계 — 앱이 그 프록시를 쓰게 설정하기" 참고)
5. **`※` 주석으로 부연** — 본문 흐름을 끊는 곁가지 지식은 문단 밖으로 뺍니다. (아래 참고)

### 비교·심화 절은 두 개념이 모두 나온 뒤에

**한쪽만 설명한 시점에 두 개념을 비교하는 표나 절을 넣지 않습니다.** 프록시 글에서
"forward proxy 가 뭔가요?"(forward proxy 대 reverse proxy 비교표 포함) 절을 처음엔
Dynamic SSH 절 바로 뒤에 뒀다가 **reverse proxy 절 뒤로 옮긴 적이 있습니다** — 그 시점엔
아직 reverse proxy 를 소개하지 않아서, 비교표가 무엇과 비교하는 것인지 알 수 없었습니다.

한쪽만 아는 시점에서는 그 개념이 다른 개념과 관련 있다는 것만 **짧게 예고**하고,
자세한 비교는 뒤로 미룹니다.

> 그러니 이렇게 정리하면 편합니다. **터널을 만드는 방식은 포트 포워딩, 그 결과로 생긴
> 물건은 forward proxy.**
>
> 우선 여기까지만 이해하고, 자세한 내용은 아래 proxy 부분에서 살펴보겠습니다.

이렇게 짧게 짚고 지나간 뒤, 두 번째 개념을 다 설명한 절 끝에 비교를 배치합니다.

### `※` 주석(note) 쓰는 법

- 본문에서 바로 다루면 흐름이 끊기는 **곁가지 지식만** 뺍니다 — 용어 정의, 배경 지식,
  "이건 왜 이렇게 부르나요" 류. 글의 핵심 논리는 note 로 빼지 않습니다.
- 형식은 문단 뒤 빈 줄 → `※` 로 시작하는 문장 → 다음 줄에 `{: .note}`.
  ```
  ※ 이렇게 도착지 주소를 바꾸는 것을 DNAT(Destination NAT)라고 부릅니다.
  {: .note}
  ```
- 여러 항목을 나열해야 하면(예: 프로그램별 설정 방법) 리스트 전체를 note 로 묶습니다.
  ```
  - **Firefox** — ...
  - **Chrome** — ...
  {: .note}
  ```
- **한 군데에 note 를 두 개 이상 잇달아 두지 않습니다.** 붙일 내용이 여럿이면 하나로
  합치거나, 본문에 남길지 뺄지부터 다시 판단합니다.
- note 안에서도 말투는 본문과 같습니다 (아래 "말투" 참고). note 라고 문어체로 바뀌지 않습니다.

### 다이어그램 — 글 구조 관점

만드는 법은 위 "다이어그램" 절을 따릅니다. 여기서는 글에 배치하는 규칙만 적습니다.

- 그림은 **그 절에서 설명하는 개념 하나당 하나**. 여러 개념을 한 그림에 욱여넣지 않습니다.
- 그림 앞에는 정의 문단, 그림 뒤에는 해설 문단이 옵니다. 그림만 덩그러니 두지 않습니다.
- 상자 `sub` 에는 실제 접속 예시(`(ex) ...`)를 넣어 감을 잡게 합니다.
- **같은 글 안의 모든 그림은 존 이름과 상황 설정을 통일합니다** (`your machine` / `server side` 등).
  한 그림에서만 다른 이름을 쓰면 독자가 같은 대상인지 헷갈립니다.

### 표 — 정리 절에서만

- 표는 **정리 절에서만** 씁니다. 본문 중간에 비교표를 넣지 않습니다 — 표는 다 설명한 뒤
  훑어보는 용도입니다.
- 행은 "다루는 층", "목적지 결정 시점", "설정하는 사람/곳", "클라이언트가 아는가" 처럼
  **같은 질문을 비교 대상 각각에 던지는 형태**로 짭니다. 대상마다 다른 기준으로 행을 짜지 않습니다.
- 표 아래에 **한 줄 요약 bullet**을 붙여 표를 다시 문장으로 풀어줍니다.

### 코드·설정 예시

- 실제로 칠 수 있는 명령/설정을 그대로 보여줍니다. ` ```bash `, ` ```nginx ` 처럼 언어 태그를 붙입니다.
- 예시의 주소·포트는 그 절의 다이어그램과 **반드시 일치**시킵니다 (예: nginx `proxy_pass`
  주소가 reverse-proxy 다이어그램의 서버 주소와 같아야 합니다).
- IP·포트는 문서용·사설 대역만 씁니다 — 공인 `203.0.113.x`, 사설 `192.168.0.x` / `10.0.0.x`,
  URL 은 `http://my-url`.

### 새 글 시작 전 체크리스트

- [ ] 여는 문단 2~4 문장 + `<!--more-->`
- [ ] `## 1., 2., 3. ...` 로 번호 매긴 절, 마지막은 항상 정리
- [ ] 각 `###` 절이 정의 → (그림) → 해설 → (설정법) → (note) 순서를 지키는가
- [ ] 비교표·심화 절이 필요한 개념을 전부 소개한 **뒤에** 오는가
- [ ] note 가 절 하나에 두 개 이상 몰려 있지 않은가
- [ ] 모든 그림의 존 이름·용어가 통일돼 있는가
- [ ] 코드 예시 주소가 다이어그램 주소와 같은가
- [ ] 말투가 `~다.` 로 딱딱하게 끝나지 않는가

---

## 말투 (중요)

글쓴이의 말투에 맞춥니다. 딱딱한 `~다.` 종결로 쓰지 마세요.

- 기본은 **존댓말** — "정리해보겠습니다", "보지 않습니다", "만들어집니다"
- 가끔 **독자에게 말을 겁니다** — "무엇이 다를까요?", "세워볼까요?", "그럼 못 재는 것은 무엇일까요?"
- 요점은 **체언으로 끊어** 리듬을 줍니다 — "가장 기본이 되는 형태.", "하나로 고정!"
- `~죠`, `~거든요`, `~고요` 같은 부드러운 종결을 섞되, 과하지 않게.

나쁜 예: "클라이언트가 할 일은 없다. 규칙은 장비에 박혀 있으므로 목적지는 하나로 고정이다."
좋은 예: "클라이언트가 할 일은 없습니다. 규칙은 장비에 박혀 있으니 **목적지는 하나로 고정**이고요."

`※` 부연 설명도 같은 말투로 씁니다.

---

## 글 쓰기

앞머리(front matter)는 프록시 글을 그대로 본으로 씁니다.

```yaml
---
title: "프록시와 포트 포워딩, 무엇이 다를까요?"
date: 2026-09-16
category: server-network
tags: [proxy, port-forwarding, ssh, socks, nginx]
cover: /assets/images/covers/proxy.svg
---
```

- 한국어 글은 `_posts/ko/YYYY-MM-DD-슬러그.md`, 영어 글은 `_posts/en/`.
  초안은 각각 `_drafts/ko/`, `_drafts/en/` (날짜 없이).
- **자동 번역은 없습니다.** 영어판은 직접 쓸 때만 생기고, `_posts/en/` 이 비어 있으면
  상단 English 링크가 자동으로 숨겨집니다.
- 주제(`category`)는 `_data/categories.yml` 에 있는 것만 씁니다:
  `cs` / `server-network` / `hpc` / `ai`. 주제를 늘리려면 그 파일에 항목을 추가하면
  홈 필터·Categories 메뉴·글 목록·배지 색까지 한꺼번에 반영됩니다.
- 대표 이미지(`cover`)를 지정하지 않으면 주제별 기본 그림이 자동으로 붙습니다.
  글 전용 그림이 필요하면 `assets/images/covers/` 에 16:9 SVG 로 만듭니다.
- 부연 설명은 문단 아래 `{: .note}` 를 붙여 작은 글씨로 답니다. `※` 로 시작합니다.
- 본문 첫 문단 뒤에 `<!--more-->` 를 넣으면 그 앞이 목록·RSS 요약이 됩니다.

자세한 것은 [WRITING.md](WRITING.md).

---

## 색과 스타일

- 사이트 팔레트는 `assets/css/style.scss` 맨 위 `:root` 한 곳에서 관리합니다.
  파랑 `#5B6CF9` → 보라 `#A855F7`, 포인트 하늘색 `#38BDF8`.
- 글꼴은 기기 기본 글꼴 스택을 씁니다. 본문용 웹폰트는 쓰지 않습니다.
  (다이어그램 손글씨 글꼴 `Gaegu` 만 예외이며, 다이어그램이 있는 글에서만 불러옵니다.)
- `.wrap` 과 같은 요소에 `padding` 축약형을 쓰면 좌우 여백이 0이 됩니다.
  세로 여백만 줄 때는 반드시 `padding-block` 을 쓰세요.

---

## 고치고 나서 확인할 것

로컬 빌드는 GitHub Pages 와 같은 Jekyll 3.x 로 돕니다.

```bash
bundle exec jekyll build --drafts   # 또는 serve --drafts
```

- 빌드가 통과하는지
- 390 / 1280px 에서 가로로 넘치지 않고 좌우 여백이 남는지
- 다이어그램이 있는 글이면 실제로 SVG 로 그려지는지 (오류 아이콘 0개)
- 내부 링크가 전부 살아 있는지

푸시하면 GitHub Actions 의 **Build check** 가 같은 빌드를 한 번 더 돌립니다.
