초안을 두는 곳입니다. 여기 있는 글은 사이트에 나오지 않습니다.

- 한국어 초안은 `_drafts/ko/`, 영어 초안은 `_drafts/en/` 에 넣습니다.
- 파일 이름에 날짜를 붙이지 않아도 됩니다.
- 다 쓰면 `_posts/ko/` 로 옮기면서 파일 이름 앞에 `YYYY-MM-DD-` 를 붙이세요.
  앞머리는 그대로 두면 됩니다.

로컬에서 초안까지 미리 보려면: `bundle exec jekyll serve --drafts`
